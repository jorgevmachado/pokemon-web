import { PASSWORD_PATTERN as SHARED_PASSWORD_PATTERN } from '@/app/shared/lib';

export const PASSWORD_PATTERN = SHARED_PASSWORD_PATTERN.source;
export const REQUIRED_FIELD_MESSAGE = 'Required field';
